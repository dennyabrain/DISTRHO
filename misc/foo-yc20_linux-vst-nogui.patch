diff -U 3 -H -b -w -i -r -N -- foo-yc20-vst-1.3.0.orig/Makefile foo-yc20-vst-1.3.0/Makefile
--- foo-yc20-vst-1.3.0.orig/Makefile	2011-09-09 15:32:51.139909805 +0100
+++ foo-yc20-vst-1.3.0/Makefile	2011-03-22 17:34:14.000000000 +0000
@@ -76,7 +76,7 @@
 	$(CXX) $(CFLAGS_use)  $(VSTSDK)/public.sdk/source/vst2.x/vstplugmain.cpp -c -o src/vstplugmain.o
 
 vsti: $(OBJS_VSTI) $(OBJS_DSP_PLUGIN) src/vsti.def
-	$(CXX) -Wall -s -shared -mwindows -static src/vsti.def $(VSTFLAGS) $(OBJS_VSTI) $(OBJS_DSP_PLUGIN) -o FooYC20.dll `pkg-config --libs cairo`
+	$(CXX) -Wall -s -shared $(VSTFLAGS) $(OBJS_VSTI) $(OBJS_DSP_PLUGIN) -o FooYC20.so `pkg-config --libs cairo`
 
 $(BIN): $(OBJ)
 
diff -U 3 -H -b -w -i -r -N -- foo-yc20-vst-1.3.0.orig/src/vsti.cpp foo-yc20-vst-1.3.0/src/vsti.cpp
--- foo-yc20-vst-1.3.0.orig/src/vsti.cpp	2011-09-09 15:32:57.309918857 +0100
+++ foo-yc20-vst-1.3.0/src/vsti.cpp	2011-09-09 15:35:47.170167992 +0100
@@ -31,7 +31,7 @@
 
 #ifdef __WIN32__
 #include <cairo-win32.h>
-#else /* __APPLE__*/
+#elif __APPLE__
 #include <Carbon/Carbon.h>
 #include <cairo-quartz.h>
 #endif
@@ -1148,7 +1148,11 @@
 #ifdef VERBOSE
 	std::cerr << "Creating the editor..." << std::endl;
 #endif
+
+#ifndef __linux__
 	setEditor(new YC20AEffEditor(this));
+#endif
+
 #ifdef VERBOSE
 	std::cout << "...done: " << editor << std::endl;
 #endif
@@ -1260,9 +1264,11 @@
 
 	*c->getZone() = value;
 
+#ifndef __linux__
 	if (editor && didChange) {
 		((YC20AEffEditor *)editor)->queueChange(index, value);
 	}
+#endif
 }
 
 float
